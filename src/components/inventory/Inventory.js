import React from 'react';
import {View, Text} from 'react-native';

const Inventory = () => {

    return(
      <View>
        <Text>Inventory</Text>
      </View>
    );
};

export default Inventory;